# nextjs-docker-cdk-demo

demo how to deploy an nextjs app with docker to fargate on AWS with AWS CDK

## commands

* `cdk deploy  --require-approval=never -c imageTag=$imageTag`  deploy to aws with a image tag in the ECR
